// Ce fichier n'est plus utilisé - L'application utilise GraphQL via Apollo Client
// Voir lib/apollo.ts et lib/graphql/ pour les requêtes et mutations GraphQL

/*
 * L'authentification se fait via :
 * - LOGIN_MUTATION et SIGNUP_MUTATION dans lib/graphql/mutations.ts
 * - AuthContext utilise Apollo Client directement
 * 
 * Les écoles se récupèrent via :
 * - SCHOOLS_QUERY dans lib/graphql/queries.ts
 */

export default null;
